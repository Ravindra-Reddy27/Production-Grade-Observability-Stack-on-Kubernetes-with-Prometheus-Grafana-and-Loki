apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: application
  labels:
    app: flask-app
spec:
  type: ClusterIP
  ports:
  - name: http-metrics
    port: 5000
    targetPort: 5000
  selector:
    app: flask-app